<UserControl x:Class="Zieschang.Net.Projects.SLJiraClient.DashboardModule.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button x:Name="SettingButton" HorizontalAlignment="Right" FontSize="8" 
                    Grid.Column="1" Grid.Row="0"
                >Settings</Button>
        <ListBox x:Name="TopFilter1" Grid.Column="0" Grid.Row="1">
            
        </ListBox>
        <ListBox x:Name="TopFilter2" Grid.Column="1" Grid.Row="1">
            
        </ListBox>
        
        <ListBox x:Name="TopFilter3" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2">

        </ListBox>
    </Grid>
</UserControl>
